# Database
MONGO_URI=mongodb://localhost:27017/brainhealthdb

# JWT secret -- replace with a strong random string in production
JWT_SECRET=replace-this-with-a-very-strong-secret

# Cookie / token timing (optional overrides)
ACCESS_TOKEN_EXPIRES_SECONDS=900
REFRESH_TOKEN_EXPIRES_SECONDS=604800
REFRESH_COOKIE_SECURE=False
REFRESH_COOKIE_SAMESITE=Lax
